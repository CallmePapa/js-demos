Array.dim=function (valueString,index) {
    let a=[],i;
    for(i=0;i<valueString.length;i++){
        a[i]=index;
    }
    return console.log(a);
};
let myArray=Array.dim(10,0);

Array.matrix=function (m,n,inital) {
    let a,i,j,mat=[];
}